# Unicorn Solar Server frontend

React and TypeScript control panel built with Vite.

All user-facing labels and descriptions are centralized in `src/content.ts`.

```bash
npm install
npm run dev
```

The development server proxies API requests to Solar Server on port `9001`.

Create the production bundle with:

```bash
npm run build
```
